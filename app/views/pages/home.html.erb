<!-- Navbar -->
<nav class="navbar frispy-navbar">
  <div class="container-fluid">
    <span class="navbar-brand text-white fw-bold">Frispy</span>

    <div class="d-flex">
      <% if user_signed_in? %>
        <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn login-btn" %>
      <% else %>
        <%= link_to "Log in", new_user_session_path, class: "btn login-btn" %>
      <% end %>
    </div>
  </div>
</nav>

<div class="container vh-100 d-flex flex-column text-center">

  <!-- Image -->
  <div class="text-center mt-3">
    <%= image_tag "frispy_logo.png", alt: "Frispy Banner", class: "frispy-image" %>
  </div>

  <!-- Body -->
  <div class="mt-3">
    <h2 class="fw-bold">Frispy: Your Recipe for Less Waste</h2>
    <hr class="my-4 w-50 mx-auto" />

    <p class="text-muted">
      Frispy keeps your fridge crisp and your meals fresh! Track expiry dates, cut waste, and whip up recipes with ease—Frispy is freshness made simple.
    </p>
    <hr class="mt-3 w-50 mx-auto" />

    <% if user_signed_in? %>
      <%= link_to "To Dashboard", food_items_path, class: "btn submit-btn btn-lg px-5 py-2 my-5" %>
    <% else %>
      <%= link_to "Sign up", new_user_registration_path, class: "btn submit-btn btn-lg px-5 py-2 my-5" %>
    <% end %>
  </div>

  <div class="text-start w-100 my-2">
    <h3 class="fw-bold text-center">Why choose Frispy?</h3>
    <ul class="list-unstyled mt-3 mx-auto" style="max-width: 500px;">
      <li class="mb-4">
        <strong>Expiry Tracking</strong>: Never let food go to waste.
      </li>
      <li class="mb-4">
        <strong>Custom Shelf Life</strong>: Define your food's freshness.
      </li>
      <li class="mb-4">
        <strong>Recipes Suggestions</strong>: Turn ingredients into meals.
      </li>
      <li>
        <strong>Notifications</strong>: Get alerts before food expires.
      </li>
    </ul>
  </div>

  <!-- Footer -->
  <footer class="text-center mt-5">
    <p class="text-muted">&copy; <%= Time.current.year %> Frispy</p>
  </footer>
</div>
